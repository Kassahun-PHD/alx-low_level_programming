#include <unistd.h>
#include "main.h"

/**
 * add - prints addition of a numbers
 * @i: the first number
 * @j: the second number
 * Description: function prints the sum of two numbers
 * Return: result
 */
int add(int i, int j)
{
	return (i + j);
}
