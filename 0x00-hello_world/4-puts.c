#include <stdio.h>

/**
 *main -Entry fuction
 *
 *Retun: A always retutn 0 (succees)
 *
 * */
int main(void)
{       char Str[] = "\"Programming is like building a multilingual puzzle";
	puts(Str);
	return (0);
}
